// Repetition thresholds for each mode.
// A word "graduates" from a mode when its counter reaches the max.
export const M1_MAX = 4   // flip card — show 4 times
export const M2_MAX = 12  // multiple choice — show 12 times
export const M3_MAX = 24  // matching grid — show 24 times

export const TOTAL_REPS = M1_MAX + M2_MAX + M3_MAX  // 40
